{
  "name": "careerfit-ai",
  "version": "1.0.0",
  "private": true,
  "description": "CareerFit AI — resume-to-job match analyzer",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "install:all": "npm install --prefix client && npm install --prefix server",
    "railway:build": "npm ci --prefix client && npm run build --prefix client && npm ci --omit=dev --prefix server",
    "start": "npm run db:init --prefix server && npm start --prefix server"
  }
}
